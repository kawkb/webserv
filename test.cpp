#include <iostream>
#include <string>

int     main(void)
{
    std::string str = "\n";
    if(str.empty())
    // while(std::size_t pos = str.find(" "))
    // {
    //     // std::size_t pos = str.find(" ");
    //     std::cout << pos << std::endl;
    //     std::string strLine = str.substr(0, pos + 1);
    //     std::cout << "request line:" << strLine << std::endl;
    //     str.erase(0,pos + 1);
    //     std::cout << "request:" << str << std::endl;
    // }
}